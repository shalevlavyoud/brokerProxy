# Populated by Maven resource filtering at build time.
# @...@ delimiters are expanded by the maven-resources-plugin.
app.version=@project.version@
app.build=@build.timestamp@
app.service=@project.artifactId@
